# [REQUIRED]
export PGUSER=
# [REQUIRED]
export PGPASSWORD=
# [REQUIRED]
export PGDATABASE=
# [REQUIRED]
export HARVESTER_IMAGE=
# [REQUIRED] Must match the namespace set under $DEPLOYMENT_NAMESPACE in ./env/values
# This is because we need to add the postgres secret to that namespace for the sidecar to reference login information.
export RHDH_NAMESPACE=
# [OPTIONAL] Will take the default value of 15 minutes if unset.
export FETCH_FREQUENCY=
